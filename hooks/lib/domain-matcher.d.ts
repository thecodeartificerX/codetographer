/**
 * Given a text (task prompt), find the best-matching domain name(s).
 */
export declare function matchDomains(text: string, indexPath: string): string[];
export declare function getDomainDocPath(projectDir: string, domain: string): string;
